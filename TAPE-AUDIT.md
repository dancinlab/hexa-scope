# TAPE-AUDIT — hexa-scope

## A. Audit-class ledgers

- `state/markers/*.marker` — **10 markers** (`closure_consistency_*`, `lattice_arithmetic_*`, `real_limits_anchor_*`, `run_all_*`). Identical four-verb pattern to cosmos/earth. **Class-T** fit but very light volume.
- No JSONL, no CLI log file.

## B. Identity surface

`AGENTS.md` + `LATTICE_POLICY.md` + `LIMIT_BREAKTHROUGH.md`. Identity = **observatory / scope instrument stack**. Fit for `hexa-scope/identity.tape` declaring observation-instrument axes (`obs_astronomy/`, `observatory/`).

## C. Domain.md files

Only 7 root MD files — minimal. Subdirs: `obs_astronomy/`, `observatory/`, `selftest/`, `verify/`, `tests/`, `cli/`, `docs/`, `examples/`. `obs_astronomy` and `observatory` are near-duplicate domains — consolidation candidate.

## D. Per-run/per-event history

Four-verb closure cycle is the only event stream; runs are sparse. Tape format adoption is **prescriptive** (instrument new observation-run events) rather than retrofit.

## E. Promotion candidates

- **n6 atoms** — diffraction-limit equation, photometric-zero-point constants, focal-length / aperture-ratio scaling.
- **hxc binaries** — telescope FITS frames (when observatory subdir emits).
- **n12 cube cells** — wavelength-band × aperture-tier × mount-class.

## Verdict

**LIGHT** — 10 markers + 7 root MDs + near-duplicate obs_astronomy/observatory subdirs. Smallest substantive repo in batch; tape adoption is forward-instrumentation, not compaction.
